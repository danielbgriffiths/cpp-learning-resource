#include <vector>
#include <iostream>
#include "cpp-learning-resource/algorithms/reverse-array/reverse-array.hpp"

int main(int argc, const char * argv[]) {
    std::vector<int> array_to_reverse = {1, 2, 3, 4, 5};
    std::vector<int> reversed_array = execute(array_to_reverse);
    std::cout << "Soon we will be able to interact with the functions through the CLI!";
    return 0;
}
